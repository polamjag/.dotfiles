# coloring for ls
if [ -e ~/.dir_colors ] ; then
    eval $(dircolors -b ~/.dir_colors)
fi

alias ls='ls -F --color'
alias l='ls -Ca'
alias ll='ls -lhF'
alias lla='ls -lhaF'

if [ -e /usr/bin/exo-open -o -e /bin/exo-open ] ; then
    alias open='exo-open'
fi

