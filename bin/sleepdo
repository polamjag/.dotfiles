#!/bin/sh

if [ $# -lt 3 ] ; then
  echo "usage: $0 count interval command"
  exit 1
fi

count="$1"
times="$1"
intvl="$2"
shift 2

while [ $count -gt 0 ] ; do
  echo
  echo "--> $count / $times"
  count=$(( $count - 1 ))
  $@
  sleep $intvl
done
