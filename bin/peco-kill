#!/bin/sh

if [ "$1" = "a" ] ; then
  cmd='ps aux'
else
  cmd='ps ux'
fi

target_pid=$($cmd | peco | awk '{print $2}')

if [ "$target_pid" != "" ] ; then
  echo "killing $target_pid: $(ps -p $target_pid -o comm=)"
  kill $target_pid
else
  echo 'aborted'
fi
