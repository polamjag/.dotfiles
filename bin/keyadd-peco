#!/bin/sh

ssh_dir="$HOME/.ssh"

key_to_add=`ls "$ssh_dir" | grep -v config | grep -v .pub | grep -v known_hosts | peco --prompt='PRVKEY>'`

if [ "$key_to_add" != "" ] ; then
  ssh-add "$ssh_dir/$key_to_add"
else
  echo 'abort'
  exit 1
fi

